# Whisper runtime

Release builds place a platform-native `whisper-cli` executable here during CI.

For local development, add `whisper-cli.exe` on Windows or `whisper-cli` on macOS/Linux. The executable must support `-m`, `-f`, `-otxt`, and `-of` arguments.
